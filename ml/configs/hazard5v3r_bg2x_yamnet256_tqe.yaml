general:
  project_name: stm32n6_hazard5v3r_yamnet256_development
  logs_dir: logs
  saved_models_dir: saved_models
  global_seed: 120
  gpu_memory_limit: 6
  display_figures: false
  deterministic_ops: true

operation_mode: chain_tqe

model:
  model_name: yamnet_e256
  input_shape: [64, 96, 1]
  pretrained: true

dataset:
  dataset_name: custom
  class_names:
    - background_other
    - dog_bark
    - glass_breaking
    - gunshot_gunfire
    - siren
    - thunderstorm
  file_extension: .wav
  training_audio_path: ../../datasets/hazard5v3r/audio
  training_csv_path: ../../datasets/hazard5v3r/meta/hazard5r_train.csv
  validation_audio_path: ../../datasets/hazard5v3r/audio
  validation_csv_path: ../../datasets/hazard5v3r/meta/hazard5r_validation.csv
  test_audio_path: ../../datasets/hazard5v3r/audio
  test_csv_path: ../../datasets/hazard5v3r/meta/hazard5r_development_test.csv
  quantization_audio_path: ../../datasets/hazard5v3r/audio
  quantization_csv_path: ../../datasets/hazard5v3r/meta/hazard5r_quantization.csv
  quantization_split: 1.0
  multi_label: false
  use_garbage_class: false
  n_samples_per_garbage_class: 2
  expand_last_dim: true
  seed: 120
  to_cache: false
  shuffle: true

preprocessing:
  min_length: 1
  max_length: 10
  target_rate: 16000
  top_db: 60
  frame_length: 3200
  hop_length: 3200
  trim_last_second: false
  lengthen: after

feature_extraction:
  patch_length: 96
  n_mels: 64
  overlap: 0.25
  n_fft: 512
  hop_length: 160
  window_length: 400
  window: hann
  center: false
  pad_mode: constant
  power: 1.0
  fmin: 125
  fmax: 7500
  norm: null
  htk: true
  to_db: false
  include_last_patch: false

data_augmentation:
  GaussianNoise:
    enable: true
    scale: 0.1
  VolumeAugment:
    enable: true
    min_scale: 0.8
    max_scale: 1.2
  SpecAug:
    enable: false
    freq_mask_param: 1
    time_mask_param: 1
    n_freq_mask: 3
    n_time_mask: 4
    mask_value: 0

training:
  fine_tune: false
  dropout: 0.0
  batch_size: 16
  epochs: 60
  resume_training: null
  optimizer:
    Adam:
      learning_rate: 0.001
  callbacks:
    ReduceLROnPlateau:
      monitor: val_accuracy
      mode: max
      factor: 0.5
      patience: 5
      min_lr: 0.00001
    EarlyStopping:
      monitor: val_accuracy
      mode: max
      restore_best_weights: true
      patience: 12

quantization:
  quantizer: TFlite_converter
  quantization_type: PTQ
  quantization_input_type: int8
  quantization_output_type: float
  export_dir: quantized_models

mlflow:
  uri: ../../training-runs/mlruns

hydra:
  run:
    dir: ../../training-runs/hazard5v3r_yamnet256/${now:%Y_%m_%d_%H_%M_%S}
